{
  "description": "Intercepts and enhances user prompts before submission to Claude",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'mkdir -p ~/.claude && [ ! -f ~/.claude/better-prompt.local.md ] && cp \"${CLAUDE_PLUGIN_ROOT}/examples/better-prompt.local.md.example\" ~/.claude/better-prompt.local.md 2>/dev/null || true; touch \"$CLAUDE_PROJECT_DIR\"/.prompts.json'",
            "statusMessage": "Placing config file and .prompts.json on project root",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/enhance.sh",
            "statusMessage": "Correcting and Enhancing Prompt...",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "clear|logout|prompt_input_exit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'rm -f \"$CLAUDE_PROJECT_DIR\"/.prompts.json'",
            "statusMessage": "Cleaning up...",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
